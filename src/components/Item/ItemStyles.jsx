import styled from "styled-components";

export const ItemStyle = styled.div`
  .card {
    width: 22rem;
  }

  .card-body {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  p {
    font-weight: bold;
    color: black;
  }
`;
