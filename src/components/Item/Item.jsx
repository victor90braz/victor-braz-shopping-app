import { h } from "preact";

const Item = () => {
  return (
    <div
      style={{
        width: "100%",
        minHeight: "34vh",
        border: "1px solid black",
      }}
    >
      item
    </div>
  );
};

export default Item;
