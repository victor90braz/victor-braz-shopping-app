import { h } from "preact";

const Item = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "25vh",
        border: "1px solid black",
        padding: "8px",
      }}
    >
      item
    </div>
  );
};

export default Item;
